{# List school year #}

{% extends 'admin/_base_admin.html.twig' %}
{% set menu_room = true %}
{% block content %}
    <div class="row">
        <div class="col-md-12">
            <div class="bgc-white bd bdrs-3 p-20 mB-20">
                <div class="row">
                    <div class="col-md-6">
                        <h4 class="c-grey-900 mB-20">Liste des salles</h4>
                    </div>
                    <div class="col-md-6 text-right">
                        <a href="{{ path('room_manage') }}" class="btn btn-primary">Nouveau</a>
                    </div>
                </div>
                <table id="dataTable" class="table table-striped table-bordered" cellspacing="0" width="100%">
                    <thead>
                    <tr>
                        <th>Nom</th>
                        <th>Action</th>
                    </tr>
                    </thead>
                    <tfoot>
                    <tr>
                        <th>Nom</th>
                        <th>Action</th>
                    </tr>
                    </tfoot>
                    <tbody>
                    {% for room in rooms %}
                        <tr>
                            <td>{{ room.name }}</td>
                            <td>
                                <a href="{{ path('room_manage',{'id': room.id}) }}">
                                    <i class="ti-pencil"></i>
                                </a>
                                <a href="{{ path('room_reservations_list',{'id':room.id}) }}">
                                    <i class="ti-list"></i>
                                </a>
                                <a href="{{ path('remove_room',{'id':room.id}) }}">
                                    <i class="ti-trash"></i>
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}