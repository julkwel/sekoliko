{% form_theme form 'form/custom_fields.html.twig' %}

        {{ form_start(form,{"attr":{"id":"formMat"}}) }}
        <div class="row">
            <div class="col-md-12">
                {{ form_row(form.matNom) }}
            </div>
        </div>
        <div class="box-footer">
            <input type="submit" class="btn btn-primary float-right" id="newMat" value="Enregistrer" name="new_matiere"/>
            <a href="{{ path('matiere_index') }}"
               class="btn btn-google">
                Annuler
            </a>
        </div>
        {{ form_end(form) }}