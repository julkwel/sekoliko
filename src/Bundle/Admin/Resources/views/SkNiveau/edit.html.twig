
{% form_theme form 'form/custom_fields.html.twig' %}

{{ form_start(form,{"attr":{"id":"formNiveau"}}) }}
    {{ form_row(form.niveauNom) }}
    <div class="box-footer">
        <input type="submit" class="btn btn-primary btn-block" id="addNiveau"  value="Modifier" name="update_niveau"/>
        <a data-dismiss="modal"
           class="btn btn-google btn-block">
            Annuler
        </a>
    </div>
{{ form_end(form) }}