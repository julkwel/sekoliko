
{% form_theme form 'form/custom_fields.html.twig' %}
    {{ form_start(form,{"attr":{"id":"formDiscipline"}}) }}
    <div class="row">
        <div class="col-md-12">
            {{ form_row(form.name) }}
        </div>
    </div>
    <div class="box-footer">
        <input type="submit" class="btn btn-primary float-right" id="newDiscipline" value="Enregistrer" name="new_discipline"/>
        <a href="{{ path('discipline_index') }}"
           class="btn btn-google">
            Annuler
        </a>
    </div>
    {{ form_end(form) }}